// var userInput = ('Enter your year of birth');
// alert(userInput)
//STRING CONCATINATION (+)
//which bring together two or more strings

var a = 'a';
var b = 9;
var c = a + 9 + 10;//ab (this is type casting: which is converting data from one datatype to datatype but its not supported for all casses)

console.log('This is C = ', C);
alert('C is, '+ a)

/**
 * var trick = + '100'; to convert from trick to number
 * var int = parseInt('D');you can put '20'
 * var float = parsefloat('D');
 * 
 * console.log('This int = ', int);
 * console.log('This float = ', float);
 */
