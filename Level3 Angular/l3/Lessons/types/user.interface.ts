// what makes this an interface is when we define the interface keyword.
//It is also used to  create custom Object types

// want to shair this, we use export.
export interface IUser {
    name: string;
    age: number;
    gender:string;
    address?: string;
}